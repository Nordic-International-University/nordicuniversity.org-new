export interface EventsTypes {
  title: string;
  description: string;
  date: string;
}

export interface EventsTypeProps {
  props: EventsTypes[];
  sectionTitle: string;
  sectionDescription: string;
}
